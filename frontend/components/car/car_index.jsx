import React from 'react';
import { Link, withRouter } from 'react-router-dom';
import CarIndexItem from './car_index_item';

class CarIndex extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
  }
}

export default withRouter(CarIndex);
